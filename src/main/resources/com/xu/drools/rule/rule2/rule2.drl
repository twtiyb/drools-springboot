package com.xu.drools;
import com.xu.drools.bean.Person;
rule "2"
	when
        $p : Person(age > 30);
    then
		System.out.println("hello, young " + $p.getName());
		System.out.println($p.toString());
end

query "people2"(int tarAge)
    person : Person( age > tarAge )
end